import {LandingPage} from "../components/landing/LandingPage";
import {render} from "@testing-library/react";

describe(LandingPage, () => {
    it("h1 is landing page", () => {
    
        expect(5).toEqual(5);
    });
});